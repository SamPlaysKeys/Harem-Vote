all:
  hosts:
    haremvote:
      ansible_host: YOUR_VPS_IP
      ansible_user: root
      ansible_ssh_private_key_file: ~/.ssh/id_rsa
      # Or use a specific key:
      # ansible_ssh_private_key_file: ~/.ssh/your_key

      # For non-root user with sudo:
      # ansible_user: deploy
      # ansible_become: true
      # ansible_become_method: sudo
